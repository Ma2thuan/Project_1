@model IEnumerable<Projekt_1.Model.passbook>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.balance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.startDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.endDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.term)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.user_address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.balance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.startDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.endDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.term)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.user_address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.passbook_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.passbook_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.passbook_id })
        </td>
    </tr>
}

</table>
